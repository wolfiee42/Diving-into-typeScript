const student: {
    // name: "saif siraj", //if the value is fixed, then assign as a type
    readonly name: string //if the value is fixed , assign readonly
    dept: string,
    roll: number,
    isPresent: boolean,
    bench?: number
} = {
    name: 'saif siraj',
    dept: 'Eng',
    roll: 1,
    isPresent: true,
    bench: 8
};
